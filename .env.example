# Copiar para ".env" (não versionado) e preencher.

# RunPod — pod persistente (Secure Cloud), ver runpod/README.md
RUNPOD_API_KEY=
# Imagem publicada por: docker build+push a partir de runpod/Dockerfile
RUNPOD_IMAGE=
# Id do pod persistente — deixa vazio na 1ª vez, o próprio código o cria e
# preenche este campo automaticamente. Depois disso, cada sessão só
# retoma (Start) e pára (Stop) este mesmo pod.
RUNPOD_POD_ID=

# Opcionais (têm omissão sensata em pipeline/runpod_pod_client.py):
# RUNPOD_CLOUD_TYPE=SECURE
# Community Cloud é mais barato mas deu sempre "CUDA unknown error" em vários
# hosts/GPUs diferentes (qualidade de host inconsistente) — confirmado testando
# o mesmo setup em Secure Cloud, onde funcionou de forma fiável (ver README.md).
# RUNPOD_GPU_TYPE_IDS=NVIDIA RTX A4000,NVIDIA RTX A5000,NVIDIA GeForce RTX 4090
# RUNPOD_CONTAINER_DISK_GB=40

# Anthropic — usado por pipeline/edit_pipeline.py para interpretar as
# instruções de texto ("adicionar buraco de chaveiro 50mm") em operações CAD
ANTHROPIC_API_KEY=
